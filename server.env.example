# ── server 端設定範本 ──
#
# 用法:複製一份改名為 server.env,再照你的需求改值。
#       server.env 不會進版本庫 —— 這點很重要,裡面會放位址與認證設定。
#
# 誰會讀這份:server.py(hub 本體)。client 那端讀的是 client.env,兩份不共用,
# 因為遠端機器只需要 client 設定,不該拿到伺服器的鑰匙。
#
# ★ 真正的環境變數優先於這份檔案:
#       HOST=127.0.0.1 uv run server.py     ← 這行永遠有效,不會被檔案蓋掉

# 綁哪個位址。
#   0.0.0.0   = 對整個區網開放(手機能連進來觀戰)
#   127.0.0.1 = 只聽本機(最安全)
# ⚠️ 預設是 0.0.0.0。在咖啡廳、公司、宿舍這種你不完全信任的網路上,
#    請改成 127.0.0.1,或把下面的 AUTH 打開 —— 兩者至少要做一件。
HOST=0.0.0.0
PORT=8787

# 對外公開的主機(只填 IP 或主機名,port 用上面那行 PORT)。HOST 開放區網時【建議設】。
#
# 為什麼:127.0.0.1 不是地址,是代名詞(意思是「本機」,誰念它就指誰)。
# Agent Card 有一個 url 欄位寫著「要跟我說話請打這裡」,那個網址由 hub 自己填;
# 沒設這一行它就填 127.0.0.1,遠端的程式照著打會打到【它自己那台】。
#
# 註:瀏覽器不受影響 —— 前端打的是相對路徑(/api/...),
#     瀏覽器會自動用「你開這一頁的網址」當基底。只有 Agent Card 需要絕對網址,
#     因為名片會被複製、存檔、轉手,它必須自己說明自己在哪。
# PUBLIC_HOST=192.168.1.50

# 認證。設成 on 之後,發言要帶鑰匙,冒名會被擋。
# AUTH=on

# 註:這裡曾經有 INVITE_TOKEN(邀請碼)。動態註冊機制 2026-07-27 退役 ——
#     現在名冊就是「誰的敲鈴器連著線」,沒有「加入」這個動作,所以也沒有邀請碼。

# 房間資料的根目錄(不設就用 hub_data/rooms)。
#
#   一房一資料夾:<這裡>/<房名>/{chat.jsonl,tasks.json}
#
# 為什麼可以改:任務檔是【整包蓋回去】的寫法,兩個 hub 共用同一份資料會
# 互相把對方的狀態洗掉(實際踩過)。用非預設埠號跑的實例會自動改用
# rooms-<port>,所以通常不需要手動設這一行。
# (這一行以前叫 TASKS_PATH,指的是單一個任務檔;分資料夾之後它指的東西變了,
#  名字跟著改 —— 名字不誠實比沒有名字更糟。)
# ROOMS_DIR=
