# ── server 端設定範本 ──
#
# 用法:複製一份改名為 server.env,再照你的需求改值。
#       server.env 不會進版本庫 —— 這點很重要,裡面會放邀請碼與認證設定。
#
# 誰會讀這份:server.py(hub 本體)。client 那端讀的是 client.env,兩份不共用,
# 因為遠端機器只需要 client 設定,不該拿到伺服器的鑰匙。
#
# ★ 真正的環境變數優先於這份檔案:
#       HOST=127.0.0.1 uv run server.py     ← 這行永遠有效,不會被檔案蓋掉

# 綁哪個位址。
#   0.0.0.0   = 對整個區網開放(手機能連進來觀戰)
#   127.0.0.1 = 只聽本機(最安全)
# ⚠️ 預設是 0.0.0.0。在咖啡廳、公司、宿舍這種你不完全信任的網路上,
#    請改成 127.0.0.1,或把下面的 AUTH 打開 —— 兩者至少要做一件。
HOST=0.0.0.0
PORT=8787

# 對外公開的網址。HOST 開放區網時建議設,否則遠端 client 拿到的
# Agent Card 會指向對它無效的 127.0.0.1。
# PUBLIC_URL=http://192.168.1.50:8787

# 認證。設成 on 之後,發言要帶鑰匙,冒名會被擋。
# AUTH=on

# 邀請碼。沒設 = 動態註冊功能關閉(安全的預設值)。
# 設了之後,新 agent 帶著這個碼就能自己報到加入名冊。
# INVITE_TOKEN=

# 任務狀態的存放位置(不設就用專案目錄下的 tasks.json)。
# TASKS_PATH=
