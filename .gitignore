# ── Python / uv ──
.venv/
__pycache__/
.pytest_cache/

# ── Runtime 狀態(不入版控:clone 的人不該繼承我們的即時狀態)──
#
# 兩個目錄的分工:
#   hub_data/  伺服器的資料(訊息、任務、名冊、鑰匙)
#   state/     客戶端的狀態(每個 agent 的進度書籤、敲鈴器紀錄)
hub_data/
state/

# 下面這幾行是給【舊版】用的:2026-07-26 之前這些檔案直接放在專案根目錄。
# 升級後伺服器會自動把它們搬進 hub_data/,但在那之前根目錄仍可能有殘留。
chat.jsonl
agents.json
tokens.json
tasks*.json

# ── 驗收與瀏覽器暫存 ──
.playwright-mcp/
*.png

# ── 個人環境設定(Claude Code session 本地設定)──
.claude/

# ── 環境設定檔:內容因機器而異,而且會裝位址與認證鑰匙 ──
# 只有 *.example 範本進版本庫,實際的設定檔留在各自的機器上。
client.env
server.env

# 資料檔的手動備份(清理測試房間時產生),不進版本庫
*.bak-*
*.tmp

# ── 每間聊天室自己協調出來的判準(不是產品的一部分,不入版控)──
#   rooms/<房名>_room_rule.md,例如 rooms/main_room_rule.md
#   ★ rooms/README.md 【要】進版控 —— clone 的人得知道那個空目錄是幹嘛的
rooms/*_room_rule.md
