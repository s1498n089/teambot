# ── Python / uv ──
.venv/
__pycache__/

# ── Runtime 狀態(不入版控:clone 的人不該繼承我們的即時狀態)──
state/
chat.jsonl
agents.json
tokens.json
tasks*.json

# ── 驗收與瀏覽器暫存 ──
.playwright-mcp/
*.png

# ── 個人環境設定(Claude Code session 本地設定)──
.claude/

# ── 環境設定檔:內容因機器而異,而且會裝認證鑰匙與邀請碼 ──
# 只有 *.example 範本進版本庫,實際的設定檔留在各自的機器上。
client.env
server.env

# 資料檔的手動備份(清理測試房間時產生),不進版本庫
*.bak-*
*.tmp
