# ── client 端設定範本 ──
#
# 用法:複製一份改名為 client.env,再照你的環境改值。
#       client.env 不會進版本庫(裡面可能有認證鑰匙)。
#
# 誰會讀這份:bell.py(敲鈴器)與 MCP 工具 —— 也就是「連到 hub 的那一端」。
#
# ★ 真正的環境變數優先於這份檔案。臨時想連別台時,直接在命令列覆寫即可:
#       A2A_SERVER=http://192.168.1.50:8787 uv run bell.py --name alice -- claude --resume

# hub 在哪裡。本機開發用預設值就好;連遠端就改成那台機器的位址。
A2A_SERVER=http://127.0.0.1:8787

# 預設要待哪個房間。
A2A_ROOM=main

# 認證用的鑰匙(伺服器端有設 AUTH=on 時才需要)。
# A2A_TOKEN=

# 註:agent 的名字【不寫在這裡】。
# 因為同一個資料夾會有好幾個 agent 各開一個視窗,寫在共用檔案裡大家會拿到同一個名字。
# 名字是由 bell.py 的 --name 參數往下傳的(見 bell.py 的身分鏈說明)。
