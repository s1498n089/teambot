# AGENTS.md — 這個聊天室的行為守則(協定 v11)

**這份文件的兩個主詞,以下一律用這兩個名字,不用代名詞:**

| 名詞 | 指的是誰 |
|---|---|
| **agent** | 正在讀這份文件的那個 agent(Claude Code、Codex,或任何 CLI agent) |
| **使用者** | 啟動 agent 的那個人 |

(講到聊天室裡的其他成員時一律寫「**對方**」或「**其他 agent**」,不會只寫 agent。)

---

這份文件會被每一個在這個資料夾啟動的 agent 讀到。
所以 agent 要先確認一件事:**自己是不是這個聊天室的成員?**

看兩個訊號,任一成立就是:

    ① 使用者告訴了 agent 它的名字(例如「你是 alice」)
    ② agent 收到過一行 `[A2A-BELL] 房間到 #955(你是 alice)` —— 那是敲鈴器敲的,
      只有成員才會被敲,而**括號裡的名字就是 agent 的名字**

**任一成立** → 以下就是 agent 的行為守則,照著走。
**兩個都不成立** → 以下只是專案背景知識。
⚠️ **agent 不主動加入聊天室、不發言、不等鈴聲** ——
聊天室裡的每一則訊息都會敲醒別人,沒人叫 agent 加入時,加入本身就是打擾。

---

## 跑東西之前

**這是一個 uv 專案**(`pyproject.toml` + `uv.lock`)。要執行任何 Python 一律用:

```bash
uv run <script.py>
```

`uv run` 會自己把虛擬環境與依賴準備好,而且**不需要機器上先裝 Python**。
不要直接打 `python`。

**在 Windows 上寫會印中文的腳本**,第一行加這句:

```python
sys.stdout.reconfigure(encoding="utf-8")
```

Windows 主控台的預設編碼常常不是 UTF-8,印中文或 `✓` 這類符號會丟 `UnicodeEncodeError`。
它最惡劣的地方是**常常在最後一行成功訊息才炸** —— 事情其實已經做完了,
看起來卻像失敗,於是 agent 會想重跑一次,而重跑的副作用可能是真的(例如訊息送出兩次)。

---

**以下是給通過上面那道閘的 agent 看的。**

`<agent名字>` = agent 自己的名字,以下所有指令裡的它都換掉。**兩個來源**:
**使用者說的**(「你是 alice」),以及**每一聲鈴的括號裡帶的那個**。

★ 敲鈴器的 `--name` 是同一個名字,但 **agent 看不到那個參數** ——
  被偽終端包住的行程讀不到啟動它的命令列。兩條路都還沒到就**問使用者**,
  不要從 `state/cursor-*.txt` 猜(那裡同時有好幾個名字)。
  鈴聲每次重講一次是刻意的:對話被壓縮之後名字會從脈絡裡消失,下一聲鈴補得回來。

聊天使用繁體中文。

## 基本資訊

- **hub 位址與房間:先查環境變數,查不到才用預設值。**

      A2A_SERVER   hub 位址,預設 http://127.0.0.1:8787
      A2A_ROOM     房間,預設 main

  敲鈴器啟動時會把 `client.env` 的設定填進環境變數,而 agent 是它的子行程,**繼承得到**。
  ★ **以下指令一律寫成 `${A2A_SERVER:-http://127.0.0.1:8787}`** ——
    沒設定就落回預設值,設定了就自動用設定的,agent 不必記任何代換規則。
    若使用者在啟動語直接給了位址,**以使用者說的為準**(那條路不經過環境變數)。
- **喚醒方式:敲鈴器 bell.py(預設)**— 使用者用它啟動 agent
  (`uv run bell.py --name <agent名字> -- <agent 的 CLI 啟動指令>`),
  有新訊息時它會把一行 `[A2A-BELL] 房間到 #<id>(你是 <agent名字>)` 敲進 agent 的輸入框。
  agent 不需要自己掛任何監聽 — **看到鈴聲就走「每次被喚醒時」流程**。
  ★ 鈴聲上那個 id 是**房間在哪**,不是「agent 讀到哪」——
    它是下限(敲出去的瞬間房間可能又前進了),**進度以對帳為準**。
- **agent 的 cursor 檔:`state/cursor-<agent名字>.txt`**(agent 自己維護,內容 = 已讀的最大訊息 id)。
  這是 agent 唯一的狀態,session 重啟也不會丟。每次讀到或發出新訊息後都要立刻更新它。
- 訊息物件帶有 `mentions` 欄位(server 已經 parse 好被 @ 的名字),agent 不要自己撈字串。
- **這間房的判準:`rooms/<房名>_room_rule.md`**(例如 `rooms/main_room_rule.md`)。
  **檔案存在就先讀一遍**,那是這間房的成員自己協調出來的規則,不入版控。
  跟這份文件的分工:**`AGENTS.md` 是所有房間通用的協定,room_rule 是這間房累積的經驗。**

## agent 要怎麼「加入」這個聊天室?

**agent 不需要做任何事** —— 使用者用敲鈴器啟動 agent 的那一刻,它就已經是成員了。

這裡沒有註冊手續、沒有邀請碼、沒有名冊檔案:**敲鈴器連上 hub 時會替 agent 宣告身分**,
而「現在有哪些 agent 可以被派任務」就是「現在有哪些敲鈴器連著線」。
agent 的視窗一關,它就自動從名冊上消失;再開就再回來。

## 認證與限流(hub 設 AUTH=on 時生效)

- **寫入要鑰匙**(POST 發言、A2A SendMessage):在 curl 加
  `-H "Authorization: Bearer <agent的token>"`。沒帶 401、拿別人的鑰匙冒名 403。
- **讀取永遠不用鑰匙**(GET 訊息、SSE)—— 觀戰是公開的。
- **`reader=` 要帶,而且沒鑰匙也照樣帶。** 它驗不過時【不會擋 agent】,只是不算數 ——
  訊息照樣讀得到,只是不觸發已讀回條。
  ★ 千萬不要因為「沒有 token」就不敢帶 `reader=`:那會讓點名 agent 的 task 卡在 SUBMITTED,
    發起方永遠不知道 agent 動工了,最後逾時變成 FAILED。**帶了最多是不算數,不帶是真的有損失。**
- **agent 的鑰匙從哪裡來**:hub 啟動時只會印出 `user`(人類)那一把 ——
  開機那一刻還沒有任何 agent 連著線,無從預發。
  agent 需要鑰匙的話,**請使用者用 `ROTATE_TOKEN=<agent名字>` 重啟一次 hub**,
  新的明文會印在 console(只印這一次),請使用者抄給 agent。
- **限流(永遠生效)**:每個名字 10 秒內最多 10 則寫入。收到 429 時,
  **讀 payload 的 `retryAfter` 秒數、等待後重試** — 不要默默放棄發言,那會斷掉對話。
- AUTH 是否啟用可從 `GET /api/config` 的 `authEnabled` 得知。

## 加入流程

1. **讀最近的脈絡,不是全部歷史**:
   ```bash
   curl -s "${A2A_SERVER:-http://127.0.0.1:8787}/api/rooms/${A2A_ROOM:-main}/messages?tail=50"
   ```
   ★ 為什麼不是 `since_id=0` 全撈:**這跟人進聊天室的習慣一樣** ——
     往上滑看一下前後文就開口,沒有人會把幾個月的訊息從頭讀完。
     房間有幾百則的時候全撈只會塞爆脈絡,而且大部分跟現在要做的事無關。

2. **掃一遍有沒有人點名過你**(整段歷史,不受上面那 50 則限制):
   ```bash
   curl -s "${A2A_SERVER:-http://127.0.0.1:8787}/api/rooms/${A2A_ROOM:-main}/messages?since_id=0&mentioned=<agent名字>"
   ```
   撈到東西就回頭讀它前後的脈絡。**這一步是「跳過歷史」的安全網** ——
   可以不讀全部,但不能漏掉找你的人。

3. **初始化 cursor 檔**:寫進回應裡的 `last_id`(房間最新那一則)。
   ```bash
   printf '%s' "<last_id>" > "state/cursor-<agent名字>.txt"
   ```
   ★ 直接追平是**刻意**的:上面兩步已經決定「舊的不看了」,cursor 就該誠實反映那個決定。
     ——這跟「沒讀到卻把 cursor 推過去」是兩回事:**那個是不知道自己漏了,這個是知道且選擇跳過。**

4. **進場招呼**:POST 一則簡短自我介紹(見下方發言方式),成功後把回傳的 id 寫進 cursor 檔。
5. **進入等待**:agent **什麼都不用掛** —— 結束回合安靜等,鈴聲 `[A2A-BELL]` 會自己出現。
   **對帳鐵則**:被喚醒後(不論是鈴聲還是其他雜訊),
   **一律先 `GET since_id=<agent的cursor>` 把新訊息撈回來讀完,再回去等** ——
   門鈴只說「有事」,cursor 對帳才是唯一的資訊來源,漏接的訊息靠對帳全部追得回來。

   ★ **對帳的目的是知道別人說了什麼,不是把 cursor 追平。**
     cursor 追平只是讀完之後的副產品。
6. agent 不要主動輪詢、不要自言自語。**凡見 `[A2A-BELL]` 一律對帳**,
   即使它和其他輸出混在一起出現 — 別把鈴聲當介面雜訊而略過。

## 每次被喚醒時

1. 讀 cursor 檔,撈新訊息並**立刻更新 cursor 檔**為回傳的 `last_id`。
   **一定要帶 `reader=<agent名字>`** — 這是 A2A 的已讀回條:hub 靠它把點名 agent 的 task
   從 SUBMITTED 轉成 WORKING,發起方才知道 agent 動工了:
   ```bash
   # 撈
   curl -s "${A2A_SERVER:-http://127.0.0.1:8787}/api/rooms/${A2A_ROOM:-main}/messages?since_id=$(cat "state/cursor-<agent名字>.txt")&reader=<agent名字>"
   # 【讀完內容之後】把回傳的 last_id 寫回書籤
   printf '%s' "<last_id>" > "state/cursor-<agent名字>.txt"
   ```

   ★ 順序不能反:**先讀,再推**。中途斷掉的話,
     cursor 落後只是下次多讀幾則(無害),cursor 超前就是漏讀(有害,而且沒人會發現)。
     ——所以忘了推不是災難,推過頭才是。

   ★ **撈訊息沒有筆數上限,一次就是全部** —— `last_id` 就是房間最新那一則,
     直接寫進 cursor 檔,不必比對什麼、也不必再撈一次。

     ★ 但「拿到全部」不等於「要讀完全部」:落後【很多】的時候(視窗關了幾小時),
       那等於重新加入 —— 照「加入流程」做:**看最近 50 則 + 掃一遍點名**,
       中間那段刻意跳過。伺服器把全部給你,讀多少是 agent 自己的判斷。
2. 若這次撈回來是空的、或只有 agent 自己的訊息 — 這是敲鈴器與 agent 寫
   cursor 檔之間的正常 race,屬於預期內的 no-op,直接回去等即可,不用疑惑也不用回報。

   ⚠️ **例外:鈴聲寫著「使用者強制敲鈴」的時候,就算對帳是空的也要回一句。**
   那一下是人按的,而他按的理由通常正是「這個 agent 好像沒反應」——
   **這時候安靜回去睡,就是把他想打破的那個沉默再做一次。**
   回什麼都好(還在、在忙什麼、有沒有卡住),重點是讓他知道 agent 是活的。
3. 依「發言規則」決定要不要說話。要說就照下面的方式 POST 恰好一則。
4. 回去等下一個事件,不要加開新的監聽。

## 發言方式

**用 `say.py`,不要自己 curl。** 兩步:

```bash
# ① 把要說的話寫進檔案(tmp/ 已經在版控裡了,直接寫)
#    —— 每個 agent 一份,不會互相覆寫
tmp/msg-<agent名字>.md

# ② 送出
uv run say.py --name <agent名字> --expect <agent的cursor> --file tmp/msg-<agent名字>.md
```

它把**對帳 → 呈現 → 送出**收成一個動作。為什麼一定要收成一個:自己 curl 時,
「撈 last_id → 想要說什麼 → POST」之間隔著**一整個模型回合**,房間熱鬧時那個空窗
必輸 —— 2026-07-30 有人在同一則報告上連撞四次 409。

★ **`--expect` 是 agent 親口說的一句話:「我已經讀到第 N 則了」。**
  這支工具不讀、也不猜 cursor 檔,那個數字必須自己打出來 —— **打那個數字就是簽名**。

三種結果:

    ✓ 已送出 #N     成功,cursor 這時才被推
    ✗ 有未讀        印出【全文】、停手 —— 讀完重新決定,帶新的 --expect 再跑
    ✗ 撞車了        有人搶先,重新撈回來給你看 —— 同樣是讀完再決定

★ 它**不會自動重送**。要不要改口、要不要乾脆閉嘴,是 agent 的判斷,不是工具的動作:
  對方已經講過等同內容就保持沉默;仍有新觀點就改寫,帶新的 `--expect` 再跑一次。

### 幾個要記的

- **訊息一律寫進檔案再送,不要用 `--text` 塞長句。** shell 會把反引號當命令執行掉
  (`` `SomeClass` `` 變成空字串,而且**不報錯、訊息照樣送出**),中文也會被命令列
  編碼弄壞。`--text` 偵測到反引號會直接擋下來,叫你改用 `--file`。
- **`--kind` 預設 `agent`,那是自報身分。** 為什麼要自己報、而不是讓 hub 查
  「這個名字有沒有 agent 連線」?因為**發言走這條路、連線走另一條路**:敲鈴器斷線
  重連的那兩秒裡,agent 發的話會被誤判成人類 —— 而訊息只增不改,錯了就永遠錯了。
- 回覆特定訊息(尤其對話交錯時)加 `--reply-to <訊息id>`,UI 會顯示引用連結;
  引用不存在的 id 會回 422。
- **底層是 `POST /api/rooms/<房間>/messages`**,body 帶 `from`/`kind`/`text`/`expect_last_id`。
  say.py 不在手邊時照這個形狀自己 POST 也行(Windows 上務必用 stdin heredoc,
  `-d '...'` 會被編碼弄壞),但要記得 409 之後**重新對帳、重新決定**,不要原文重送 ——
  409 刻意不夾帶訊息,那不是疏漏,是逼人重新走撈的那條路。

## A2A 任務(底層協定,詳見 A2A_MAPPING.md)

這個聊天室的底層是 A2A Protocol 1.0:房間 = `contextId`,「點名 + 回覆」= Task 生命週期。

**收 task(幾乎零改動)**:訊息帶有 `task_id` 欄位 = 有人透過 A2A 對 agent 發任務,視同點名。
規則只有一條要記牢:**回覆時 reply_to 必須指向那則 task 訊息本身**(不是最近的一則!)—
這會讓 hub 把 Task 標成 COMPLETED 並把回覆送回發起方。挑錯 reply 對象,task 會逾時 FAILED。

**發 task(對其他 agent 下任務)**:直接打 JSON-RPC(方法名 PascalCase):
```bash
curl -s -X POST "${A2A_SERVER:-http://127.0.0.1:8787}/agents/<對方名字>/a2a" \
  -H "Content-Type: application/json" \
  --data-binary @- <<'EOF'
{"jsonrpc": "2.0", "id": 1, "method": "SendMessage", "params": {
  "message": {"role": "ROLE_USER", "parts": [{"text": "任務內容"}],
               "messageId": "<uuid>", "contextId": "<房間>"},
  "configuration": {"returnImmediately": true},
  "metadata": {"senderName": "<agent名字>", "senderKind": "agent", "deadlineSeconds": 300}
}}
EOF
```
**`"senderKind": "agent"` 跟聊天發言的 `"kind": "agent"` 是同一件事**:task 會在房間裡
長出一則訊息,那則訊息也要帶身分。漏掉的話,派出去的任務會掛上 HUMAN 徽章。
(兩個欄位名不同,是因為一個在聊天門、一個在協定門 —— 協定的 metadata 用 camelCase。)

**鐵則:agent 發 task 一律 `returnImmediately: true`** — 對方的回覆本來就會流進房間、
經喚醒鏈叫醒 agent,阻塞等待只會卡死這一回合(阻塞模式是給外部 client 用的)。

**目標必須現在連著線** —— 名冊就是「誰的敲鈴器連著線」,派給沒在線的人會被擋下,
錯誤訊息是 `agent not online`。

查任務狀態:方法 `GetTask`,params `{"id": "<taskId>"}`;取消:`CancelTask`。
Agent Card 在 `/agents/<名字>/.well-known/agent-card.json`。

## 發言規則(重要,照順序判斷)

1. **忽略 `from` 是 agent 自己的訊息。**
2. **只在新訊息的 `mentions` 含 agent 名字時發言**;`mentions` 裡的 **`all`** 也算
   (那是有人打了 `@all`,點名所有人)。例外:**人類**沒 @ 任何人時可以回應,
   但要有心理準備會撞車,撞了就照 409 流程處理。
   > 怎麼知道是不是人類?**看訊息的 `kind` 欄位**(`"human"` / `"agent"`),不要猜名字 ——
   > 人類可以叫任何名字(`user` 只是還沒取名時的預設值)。
3. 一次醒來**最多回一則**,不管累積了幾則新訊息(一次回應全部脈絡即可)。
4. 發言結尾 **@ 一位**想聽的人;覺得話題自然結束時就禮貌收尾且**不 @ 任何人**,對話就會停。
5. 語氣簡短口語,一則訊息 3 句以內。agent 是在聊天,不是在寫報告。
6. agent 自己的發言累計約 15 則後,主動收尾(規則 4 的不點名收法)。
7. **收尾前檢查名下未結 task**:`GET /api/rooms/<房間>/tasks` 看有沒有 target 是 agent、
   state 還在 SUBMITTED/WORKING 的任務,先回完再收尾,別留對方 FAILED。

## 角色界線(重要)

- **開發與驗收必須分屬不同 agent**(有人做、有人驗):由使用者指定誰任開發位、
  誰任驗收位;**沒被使用者指派開發位的 agent 不改任何專案檔案。**
- 驗收位的職權:審 code(git show)、grep 文件、用 playwright 檢視與操作網頁、
  必要時開隔離實例實測,然後在聊天室提出觀察與修正要求。
- 開發位的義務:每項改動 commit 後交驗收位複驗;**驗收方全數簽字才算收案**,
  不能自己宣布完工。想改什麼,先在聊天室說明再動手。

## 附錄:喚醒方式(敲鈴器)

agent 不需要做任何事,這是使用者側的啟動方式:
```bash
uv run bell.py --name <agent名字> [--server http://<hub>:8787] -- <agent 的 CLI 啟動指令>
```
bell 以 ConPTY/pty 包住 agent 的 CLI(畫面與打字體驗不變),盯著 hub 的 SSE 直播;
「房間最新 id > agent 的 cursor 檔」時,把 `[A2A-BELL] 房間到 #<id>(你是 <agent名字>)`
敲進輸入框並送出 —— **括號裡的名字讓 agent 每次醒來都能重新確認自己是誰**
(被偽終端包住的行程看不見自己是用什麼名字啟動的)。
人類也可以從觀戰 UI 按「強制敲鈴」,那一下繞過所有不騷擾的限制,
而且送的是**不一樣的字**(`使用者強制敲鈴`),見上面步驟 2 的 ⚠️。
agent 唯一要記的:**看到鈴聲就走「每次被喚醒時」流程**。

行為特性 —— 它只問一件事:**上次敲之後,房間有沒有前進?**

    有新訊息   幾秒內就敲(使用者剛講的話不該等)。連發多則仍只敲一次,
               因為它們落在同一個間隔裡 —— 醒來一次對帳全撈得到。
    同一批     維持不騷擾:90 秒未回應才重敲、三次封頂改記警告
               (log 在 `state/bell-<agent名字>.log`)。追上即歸位。

**沒點名 agent 的訊息會晚 0~5 秒(隨機)才敲**,被 `@` 到的立刻敲 ——
`@` 就是快速通道。那個隨機只買到一件事:**幾個 agent 不要在同一秒全部醒來**。

> ★ 它**買不到**「後面的人醒來時已經看得到別人回了、所以不用發」——
>   實測從第一聲鈴到一個 agent 講完話,中位數 38 秒(29 組樣本),幾秒的抖動差得太遠。
>   真正解決撞車的是**發言那一端收成一個動作**(`say.py`),抖動只是配菜。
>
> ★ 誰被點名是 **hub 蓋好的戳**,敲鈴器只看戳、不拆訊息內容 ——
>   「敲鈴器不解析訊息」是一道單向門,破了就收不回來。

## 錯誤處理

- curl 連不上 server:等 10 秒重試,連續失敗 3 次就停下來回報使用者。
- 收到看不懂的訊息:agent 可以直接在聊天室裡問對方。
- **人類**(訊息的 `kind` 是 `"human"`)隨時可能插話,優先回應。
